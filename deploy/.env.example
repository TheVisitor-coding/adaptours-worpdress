# Copier en .env puis renseigner les valeurs réelles (ne pas committer .env).

# ─── Déploiement du thème vers Infomaniak (deploy/deploy-theme.sh) ───
# rsync-over-SSH du dossier du thème. DEPLOY_PATH est relatif au home SSH.
SSH_HOST=
SSH_USER=
SSH_PORT=
DEPLOY_PATH=
# Clé privée dédiée (optionnel si déjà géré via ~/.ssh/config).
SSH_KEY=

# ─── Démo Docker VPS (deploy/docker-compose.yml) — hérité, indépendant du bloc ci-dessus ───
# Sous-domaine public de la démo. DNS : enregistrement A -> IP du VPS.
# Le certificat TLS est géré par Nginx Proxy Manager (Let's Encrypt).
WP_DOMAIN=demo-adaptours.example.fr

# Base de données (interne au réseau Docker, jamais exposée).
DB_NAME=adaptours
DB_USER=adaptours
DB_PASSWORD=change-me-strong-password

# Préfixe des tables : doit correspondre au dump importé (wp-env = wp_).
DB_PREFIX=wp_
